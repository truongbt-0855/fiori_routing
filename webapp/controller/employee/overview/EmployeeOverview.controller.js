sap.ui.define(['fiori/routing/controller/BaseController'], (BaseController) => {
    'use strict';

    return BaseController.extend('fiori.routing.controller.employee.overview.EmployeeOverview', {
        
    });
});
